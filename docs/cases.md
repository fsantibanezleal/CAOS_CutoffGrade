# Cases

The CATEGORY taxonomy + the 10-case coverage matrix live in
**[cases/README.md](cases/README.md)** (a single matrix page; there are no per-case pages yet). Each case carries a
category, params, an expected band, and a real|synthetic flag; the App shows one case, Experiments/Benchmark
summarize across categories.
