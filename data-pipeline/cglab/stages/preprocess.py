"""Stage 1, preprocess (light): validate each case's deposit + economics through CONTRACT 1 (cglab.io.contract). The
cases ARE the inputs (no raw files to clean); this proves the ingestion gate and carries the flags into the manifest."""
