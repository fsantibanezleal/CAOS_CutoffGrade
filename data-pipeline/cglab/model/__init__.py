"""The learned-model definitions (feature contracts + torch architectures) for the offline heavy lane — see
``model/learned.py``. NOTE: this product's analytic engine is TypeScript (``frontend/src/lane/``), shared by the
browser and the offline bake; there is NO Python engine re-port in this package."""
